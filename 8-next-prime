public Integer nextPrime(Integer num) {
    //code here
    for(Integer i = num+1; ;i++){
        for(integer j=2; i<i ;i++){
            if(math.mod(num,i) ==0){
                return i; 
            }
        }
       
    }
}