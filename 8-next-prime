public Integer nextPrime(Integer num) {
    //code here
    Integer result;
    for(integer i =num+1;;i++)
    { 
        for(integer j=num; j>num ;i++){
            if(math.mod(num,j) !=0){
             result= j;
            }
        }
    }
    return result;
}