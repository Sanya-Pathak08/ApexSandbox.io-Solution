public Integer nextPrime(Integer num) {
    //code here
    for(Integer i = num+1; ;i++){
        if(isPrime(i))
        {
            return i;
        }
    }

    
    
}
public boolean isPrime(integre num){
        for(integer i=2; i<num ;i++){
            if(math.mod(num,i) ==0){
            return false;
            }
        }
        return true;
    }